plugdo.integration("get-file", (message, send) => {
  let response = {};

  // console.log(message)
  // plugdo.collect("readFile").get(message, (data, err) => {
  //   if (err) {
  //     send({}, err);
  //   } else {
  //     response.result = data;
  //     send(response);
  //   }
  // });
})
